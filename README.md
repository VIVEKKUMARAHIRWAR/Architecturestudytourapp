
  # Architecture Study Tour App

  This is a code bundle for Architecture Study Tour App. The original project is available at https://www.figma.com/design/bfJefgmZmMur00x47URp9I/Architecture-Study-Tour-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  